@page
@model KD_Gadgets.Pages.Client.Orders.DetailsModel
@{
}
<h2 class="rounded p-2 my-2 text-white text-center" style="background-color : #3c6478;">Order Details</h2>



<div class="border rounded p-3 mb-3">
	<h3>Order</h3>

	<table class="table">
		<tr>
			<td>Order ID</td>
			<td>@Model.orderInfo.id</td>
		</tr>
		<tr>
			<td>Order Date</td>
			<td>@Model.orderInfo.orderDate</td>
		</tr>
		<tr>
			<td>Delivery Address</td>
			<td>@Model.orderInfo.deliveryAddress</td>
		</tr>
		<tr>
			<td>Payment Method</td>
			<td>@Model.orderInfo.paymentMethod</td>
		</tr>
		<tr>
			<td>Payment Status</td>
			<td>
				@Model.orderInfo.paymentStatus
			</td>
		</tr>
		<tr>
			<td>Order Status</td>
			<td>
				@Model.orderInfo.orderStatus
			</td>
		</tr>
	</table>

</div>

<table class="table">
	<thead>
		<tr>
			<th>Description</th>
			<th style="width:100px">Units</th>
			<th style="width:100px">Amount</th>
		</tr>
	</thead>
	<tbody>
		@{
			decimal subtotal = 0;
		}
		@foreach (var item in Model.orderInfo.items)
		{
			subtotal += item.unitPrice * item.quantity;
			<tr>
				<td>
					<h3>@item.productInfo.ProductName</h3>
					@item.productInfo.Category<br>
					@(item.unitPrice)$
				</td>
				<td>@item.quantity</td>
				<td>@(item.unitPrice * item.quantity)$</td>
			</tr>
		}
	</tbody>
</table>
<div class="text-end m-3">
	<strong>Subtotal:</strong>@(subtotal)$<br />
	<strong>Shipping Fee:</strong>@(Model.orderInfo.shippingFee)$<br />
	<strong>Total:</strong>@(subtotal + Model.orderInfo.shippingFee)$<br />
</div>